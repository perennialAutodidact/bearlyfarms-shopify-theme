<style>
  .hero-column {
    width: 100%;
    display: flex;
    flex-direction: column;
    justify-content: flex-end;
    align-items: center;
    padding-top: 2rem;
    text-align: center;
  }

  @media screen and (min-width: {{settings.breakpoint_xl}}px) {
    .hero-column {
      text-align: left;
      flex: 1;
      width: 100%;
    }

    .hero-column:nth-child(1) {
      /* padding-right: 10rem; */
    }

    .hero-column:nth-child(2) {
      /* padding-left: 10rem; */
    }
  }
</style>

{% content_for 'blocks' %}

{% schema %}
{
  "name": "Hero Column",
  "blocks": [{ "type": "@theme" }],
  "class": "hero-column",
  "settings": [],
  "presets": [
    {
      "name": "Hero Column",
      "blocks": [{ "type": "logo-header-group" }, { "type": "heading" }, { "type": "button" }]
    }
  ]
}
{% endschema %}
